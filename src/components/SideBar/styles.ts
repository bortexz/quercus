import styled from 'styled-components';

export const SideBar = styled.div`
  padding-top: 1em;
  position: sticky;
  width: 8rem;
  > div {
    position: fixed;
    top: 12px;
    bottom: 0;
    width: 8rem;
  }
`;
